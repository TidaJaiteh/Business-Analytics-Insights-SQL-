# 📊 Business Analytics Insights — SQL

A collection of SQL queries designed to extract actionable business insights from a relational e-commerce database. The project covers foundational to advanced SQL techniques including joins, aggregations, CTEs, window functions, and customer segmentation.

---

## 📌 Project Overview

This project simulates a real-world business analytics scenario where a data analyst is tasked with answering key business questions using SQL. The database consists of customers, orders, order items, and products — a structure common in e-commerce and retail environments.

---

## 🛠️ Tools & Technologies

- **PostgreSQL** — database management and query execution
- **SQL** — data querying and analysis

---

## 🗄️ Database Schema

```
customer
├── customer_id (PK)
└── name

orders
├── order_id (PK)
├── customer_id (FK → customer)
└── order_date

order_items
├── order_item_id (PK)
├── order_id (FK → orders)
├── product_id (FK → products)
└── quantity

products
├── product_id (PK)
├── product_name
└── price
```

---

## 📁 Project Structure

```
Business-Analytics-Insights-SQL/
├── basic_queries.sql        # Foundational business queries
├── advanced_queries.sql     # Advanced analytics queries
└── README.md                # Project documentation
```

---

## 🔍 Queries Breakdown

### 📂 Basic Queries

**1. Total Revenue**
Calculates the overall revenue generated by multiplying quantity by product price across all orders.
```sql
SELECT
    SUM(order_items.quantity * products.price) AS total_revenue
FROM order_items 
JOIN products 
     ON order_items.product_id = products.product_id;
```

---

**2. Revenue by Product**
Ranks products by the revenue they generate to identify best and worst performers.
```sql
SELECT products.product_name,
     SUM(order_items.quantity * products.price) AS monthly_revenue
FROM order_items
JOIN products
     ON order_items.product_id = products.product_id
GROUP BY products.product_name
ORDER BY monthly_revenue DESC;
```

---

**3. Top Customers by Spending**
Identifies the highest spending customers by joining across all four tables.
```sql
SELECT customer.name,
     SUM(order_items.quantity * products.price) AS total_spent
FROM customer
JOIN orders
     ON customer.customer_id = orders.customer_id
JOIN order_items
     ON orders.order_id = order_items.order_id
JOIN products
     ON order_items.product_id = products.product_id
GROUP BY customer.name
ORDER BY total_spent DESC;
```

---

**4. Repeat Customers**
Finds customers who have placed more than one order — a key retention metric.
```sql
SELECT customer.name,
     COUNT(orders.order_id) AS total_orders
FROM customer
JOIN orders
     ON customer.customer_id = orders.customer_id
GROUP BY customer.name
HAVING COUNT(orders.order_id) > 1
ORDER BY total_orders DESC;
```

---

**5. Monthly Sales Trend**
Tracks revenue month by month to identify seasonal patterns and growth trends.
```sql
SELECT
    DATE_TRUNC('month', orders.order_date) AS month,
    SUM(order_items.quantity * products.price) AS revenue
FROM orders
JOIN order_items
     ON orders.order_id = order_items.order_id
JOIN products
     ON order_items.product_id = products.product_id
GROUP BY month
ORDER BY month;
```

---

### 📂 Advanced Queries

**1. Top Products Ranked by Revenue (Window Function)**
Uses a CTE to calculate product revenue then applies RANK() window function to assign revenue rankings without losing other rows.
```sql
WITH ProductRevenue AS (
    SELECT
        p.product_name,
        SUM(oi.quantity * p.price) AS revenue
    FROM products p
    JOIN order_items oi
        ON p.product_id = oi.product_id
    GROUP BY p.product_name
)
SELECT
    product_name,
    revenue,
    RANK() OVER (ORDER BY revenue DESC) AS revenue_rank
FROM ProductRevenue;
```

---

**2. Customer Segmentation (CASE Statement)**
Segments customers into High, Medium, and Low value tiers based on total spending — useful for targeted marketing campaigns.
```sql
WITH CustomerSpending AS (
    SELECT
        c.customer_id,
        c.name,
        SUM(oi.quantity * p.price) AS total_spent
    FROM customer c
    JOIN orders o
        ON c.customer_id = o.customer_id
    JOIN order_items oi
        ON o.order_id = oi.order_id
    JOIN products p
        ON oi.product_id = p.product_id
    GROUP BY c.customer_id, c.name
)
SELECT
    customer_id,
    name,
    total_spent,
    CASE
        WHEN total_spent >= 1000 THEN 'High Value'
        WHEN total_spent >= 500 THEN 'Medium Value'
        ELSE 'Low Value'
    END AS customer_segment
FROM CustomerSpending
ORDER BY total_spent DESC;
```

---

**3. Revenue Contribution by Product (%)**
Calculates each product's percentage contribution to total revenue using a window function — helps identify which products drive the most business value.
```sql
WITH ProductRevenue AS (
    SELECT
        p.product_name,
        SUM(oi.quantity * p.price) AS revenue
    FROM products p
    JOIN order_items oi
        ON p.product_id = oi.product_id
    GROUP BY p.product_name
)
SELECT
    product_name,
    revenue,
    ROUND(
        (revenue * 100.0) /
        SUM(revenue) OVER (),
        2
    ) AS revenue_percentage
FROM ProductRevenue
ORDER BY revenue DESC;
```

---

## 💡 Key Business Questions Answered

| # | Business Question | Query Type |
|---|---|---|
| 1 | What is the total revenue generated? | Aggregation |
| 2 | Which products generate the most revenue? | GROUP BY + ORDER BY |
| 3 | Who are our highest spending customers? | Multi-table JOIN |
| 4 | Which customers are returning buyers? | HAVING clause |
| 5 | How does revenue trend month by month? | DATE_TRUNC |
| 6 | How do products rank by revenue? | CTE + Window Function |
| 7 | Which customers are most valuable? | CTE + CASE Statement |
| 8 | What % of revenue does each product contribute? | CTE + Window Function |

---

## 🚀 Advanced SQL Concepts Demonstrated

- ✅ Multi-table JOINs (up to 4 tables)
- ✅ Aggregate functions (SUM, COUNT)
- ✅ GROUP BY and HAVING clauses
- ✅ Common Table Expressions (CTEs)
- ✅ Window Functions (RANK, SUM OVER)
- ✅ CASE statements for segmentation
- ✅ Date functions (DATE_TRUNC)
- ✅ Percentage calculations

---

## 👤 Author

**Tida Jaiteh**
Information Systems Student
🔗 [GitHub](https://github.com/TidaJaiteh)
